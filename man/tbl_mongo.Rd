% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tbl_mongo.R
\name{tbl_mongo}
\alias{tbl_mongo}
\title{Create a lazy MongoDB table}
\usage{
tbl_mongo(collection, name = NULL, schema = NULL, executor = NULL)
}
\arguments{
\item{collection}{A \code{mongo_src} object or a \code{mongolite::mongo()}-like object.}

\item{name}{Optional collection name when \code{collection} is not already a \code{mongo_src}.}

\item{schema}{Optional character vector describing known fields.}

\item{executor}{Optional executor function for compiled pipelines.}
}
\value{
A \code{tbl_mongo} object.
}
\description{
Create a lazy MongoDB table
}
\details{
Supplying \code{schema = ...} is the most reliable way to make field references
explicit, especially for dotted paths in nested documents. If you do not want
to write the schema manually, \code{\link[=infer_schema]{infer_schema()}} can populate it from the first
document in the collection:

\code{tbl_mongo(collection) |> infer_schema()}

This is convenient, but it only reflects one document. If the collection is
heterogeneous, fields that do not appear in the first document may still need
to be added manually.
}
\examples{
tbl <- tbl_mongo(
  list(name = "orders"),
  schema = c("status", "amount"),
  executor = function(pipeline, ...) {
    tibble::tibble(status = "paid", amount = 10)
  }
)

tbl
}
