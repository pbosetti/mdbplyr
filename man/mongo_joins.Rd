% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verbs-join.R
\name{inner_join.tbl_mongo}
\alias{inner_join.tbl_mongo}
\alias{left_join.tbl_mongo}
\alias{semi_join.tbl_mongo}
\alias{anti_join.tbl_mongo}
\title{Join a lazy Mongo query to another collection}
\usage{
inner_join.tbl_mongo(
  x,
  y,
  by = NULL,
  copy = FALSE,
  suffix = c(".x", ".y"),
  ...,
  keep = NULL,
  unnest = TRUE,
  name = NULL
)

left_join.tbl_mongo(
  x,
  y,
  by = NULL,
  copy = FALSE,
  suffix = c(".x", ".y"),
  ...,
  keep = NULL,
  unnest = TRUE,
  name = NULL
)

semi_join.tbl_mongo(x, y, by = NULL, copy = FALSE, ...)

anti_join.tbl_mongo(x, y, by = NULL, copy = FALSE, ...)
}
\arguments{
\item{x}{A \code{tbl_mongo} object (the left table).}

\item{y}{A plain \code{tbl_mongo} object (the right table): a collection reference
with a known schema and no lazy operations.}

\item{by}{A character vector of shared columns, optionally named
(\code{by = c("a" = "b")} joins \code{x$a} to \code{y$b}). When \code{NULL}, the common columns
are used.}

\item{copy}{Ignored; included for \code{dplyr} compatibility.}

\item{suffix}{Length-2 character vector of suffixes for columns that collide
between \code{x} and \code{y}.}

\item{...}{Unused.}

\item{keep}{Only \code{NULL}/\code{FALSE} is supported; join keys come from the left
table.}

\item{unnest}{When \code{TRUE} (default), the match is flattened to one row per
matched pair (\code{$lookup} + \code{$unwind}). When \code{FALSE}, matches are kept as a
nested array column.}

\item{name}{Name of the nested array column when \code{unnest = FALSE}. Defaults to
the right collection name.}
}
\value{
A modified \code{tbl_mongo} object.
}
\description{
Join a lazy Mongo query to another collection
}
\details{
Joins compile to MongoDB \code{$lookup} against a collection in the same database.
\code{inner_join()} keeps only matched rows; \code{left_join()} keeps all left rows.
}
