% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/append-stage.R
\name{append_stage}
\alias{append_stage}
\title{Append a manual MongoDB aggregation stage}
\usage{
append_stage(x, json_string)
}
\arguments{
\item{x}{A \code{tbl_mongo} object.}

\item{json_string}{A JSON string representing a single MongoDB pipeline
stage, such as \code{{"$match":{"status":"paid"}}}.}
}
\value{
A modified \code{tbl_mongo} object.
}
\description{
Appends a single raw MongoDB aggregation stage, provided as a JSON string,
after the stages generated from the current lazy query.
}
\details{
This is an escape hatch for features not yet modeled by the package. The
package does not attempt to infer schema changes introduced by manual stages.
}
\examples{
tbl <- tbl_mongo(
  list(name = "orders"),
  schema = c("status", "amount"),
  executor = function(pipeline, ...) tibble::tibble()
)

query <- append_stage(tbl, "{\"$limit\": 1}")
show_query(query)
}
