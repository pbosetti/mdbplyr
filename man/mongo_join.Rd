% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verbs-join.R
\name{inner_join.tbl_mongo}
\alias{inner_join.tbl_mongo}
\alias{left_join.tbl_mongo}
\alias{right_join.tbl_mongo}
\alias{full_join.tbl_mongo}
\alias{semi_join.tbl_mongo}
\alias{anti_join.tbl_mongo}
\title{Join two lazy MongoDB tables}
\usage{
\method{inner_join}{tbl_mongo}(
  x,
  y,
  by = NULL,
  copy = FALSE,
  suffix = c(".x", ".y"),
  ...,
  keep = NULL
)

\method{left_join}{tbl_mongo}(
  x,
  y,
  by = NULL,
  copy = FALSE,
  suffix = c(".x", ".y"),
  ...,
  keep = NULL
)

\method{right_join}{tbl_mongo}(
  x,
  y,
  by = NULL,
  copy = FALSE,
  suffix = c(".x", ".y"),
  ...,
  keep = NULL
)

\method{full_join}{tbl_mongo}(
  x,
  y,
  by = NULL,
  copy = FALSE,
  suffix = c(".x", ".y"),
  ...,
  keep = NULL,
  na_matches = c("na", "never")
)

\method{semi_join}{tbl_mongo}(x, y, by = NULL, copy = FALSE, ...)

\method{anti_join}{tbl_mongo}(x, y, by = NULL, copy = FALSE, ...)
}
\arguments{
\item{x}{A \code{tbl_mongo} object (left table).}

\item{y}{A \code{tbl_mongo} object (right table, same database as \code{x}).}

\item{by}{A character vector of column names to join on.  Use a \emph{named}
vector to join on columns with different names:
\code{by = c("left_col" = "right_col")}.  When \code{NULL} (default), the join
uses all columns that appear in both \code{x} and \code{y} schemas.}

\item{copy}{Ignored (included for dplyr compatibility).}

\item{suffix}{Ignored (column-name conflicts are not yet handled
automatically; rename columns beforehand if needed).}

\item{...}{Ignored.}

\item{keep}{Ignored (included for dplyr compatibility).}

\item{na_matches}{Ignored (included for dplyr compatibility).}
}
\value{
A modified \code{tbl_mongo} object with the join recorded in the IR.
The result schema is the union of \code{x}'s and \code{y}'s schemas (minus the
duplicate foreign key when the join keys have different names).
}
\description{
These functions implement the dplyr join verbs for \code{tbl_mongo} objects.
They compile to MongoDB \verb{$lookup} aggregation stages, so \strong{both} \code{x} and
\code{y} must be \code{tbl_mongo} objects backed by collections in the \strong{same}
MongoDB database.
}
\details{
The join is lazy: no data is fetched until \code{\link[=collect]{collect()}} is called. The
compiled aggregation pipeline can be inspected with \code{\link[=show_query]{show_query()}}.

Only \code{inner_join}, \code{left_join}, \code{semi_join}, and \code{anti_join} are supported
natively via \verb{$lookup}. \code{right_join} and \code{full_join} are not supported
because MongoDB has no native equivalent that can be expressed as a single
aggregation starting from the left collection.
}
\examples{
orders <- tbl_mongo(
  list(name = "orders"),
  schema = c("order_id", "amount"),
  executor = function(pipeline, ...) tibble::tibble()
)
customers <- tbl_mongo(
  list(name = "customers"),
  schema = c("order_id", "name"),
  executor = function(pipeline, ...) tibble::tibble()
)

q <- dplyr::inner_join(orders, customers, by = "order_id")
show_query(q)
}
